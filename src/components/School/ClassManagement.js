import React from "react";

function ClassManagement() {
  return (
    <div style={styles.container}>
      <h2>ClassManagement</h2>
      <p>ClassManagement will go here.</p>
    </div>
  );
}

const styles = {
  container: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    height: '100vh'
  }
};

export default ClassManagement;
